import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const dataSliceTest = createSlice({
  name: 'uiValues',
  initialState,
  reducers: {}
});

export const {} = dataSliceTest.actions

export default dataSliceTest.reducer